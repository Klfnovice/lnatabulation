import streamlit as st

# Get the current query parameters
query_params = st.experimental_get_query_params()

# Check if the current page is page2
if query_params.get('page', [''])[0] == 'page2':
    st.write("You are now on Page 2")

    # Add your additional form inputs here
    additional_info = st.text_input("Additional Information")

    if st.button('Submit'):
        st.success("Form submitted successfully!")
else:
    st.write("You are on the main form page")
